/**
 * Stub para dependências opcionais do jsPDF (html2canvas, canvg, dompurify).
 * Só são usadas por `doc.html()`/SVG, que este app não chama — sem o stub elas
 * entram no precache do service worker sem nunca serem carregadas.
 */
export default {};
